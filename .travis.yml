language: python
python:
- '3.8'
install:
- python --version
- pip install -U pip
- pip install -U pytest
- pip install Pillow
- pip install requests
script: pytest
deploy:
  provider: releases
  skip-cleanup: true
  api_key: $GITHUB_TOKEN
  file: "$TRAVIS_BUILD_DIR/MyStory.apk"
  overwrite: true
  on:
    branch: master